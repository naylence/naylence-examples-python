# Policy server configuration

# SSL/TLS certificates for the policy server to trust Caddy's internal CA
SSL_CERT_FILE=/caddy-data/caddy/pki/authorities/local/root.crt

# Application host and port
FAME_APP_HOST=0.0.0.0
FAME_APP_PORT=8097

# Directory containing policy files
# Policy files should be named: policy-{id}.yaml or policy-{id}.json
FAME_POLICY_DIR=/app/policies

# OAuth2 JWT authentication for policy server
# The policy server will validate tokens using the same OAuth2 server as the fabric
FAME_OAUTH2_ISSUER=https://oauth2-server
FAME_OAUTH2_AUDIENCE=fame.fabric

# Logging
FAME_LOG_LEVEL=info
