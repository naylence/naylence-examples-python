FROM naylence/agent-sdk-python

# Install additional packages required for LLM examples
RUN pip install --no-cache-dir \
    openai \
    python-dotenv \
    pillow \
    numpy \
    anthropic \
    tiktoken

WORKDIR /work
